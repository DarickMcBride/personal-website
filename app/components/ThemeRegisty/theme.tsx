import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#00acee",
    },
    secondary: {
      main: "#00EEB9",
    },
    background: {
      default: "#121212",
    },
  },
});

export default theme;
